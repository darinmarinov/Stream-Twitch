import React from 'react'
import history from '../../history'

const StreamEdit = () => {
    console.log(history)
    return(
        <div>
            StreamEdit
        </div>
    )
}

export default StreamEdit;